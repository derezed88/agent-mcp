# Secrets and credentials - NEVER commit these
.env
.env.*
!.env.example
credentials.json
token.json
gen-lang-client*.json
*service-account*.json
*-key.json

# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.eggs/
*.egg

# Logs
*.log
server.log
/tmp/

# Runtime state
.aiops_session_id
sprompt_backup/


# IDE / tool settings
.claude/
.aider*
*.aider*

# OS
.DS_Store
Thumbs.db

# Local test utilities (not for public repo)
mock_airs_server.py

# Deployment-specific system prompt personas — exclude only if they contain
# identifying operational context. Sanitized personas can be committed.

# Session history (runtime state, may contain sensitive conversation data)
session-history/

# Pinggy tunnel URLs and other local infra scripts
pinggy_urls.sh
start_pinggy_tunnel.sh

# Instance-specific auto-enrichment rules (may contain table names, patterns)
auto-enrich.json

# Instance-specific database configuration (table names, database name)
db-config.json
